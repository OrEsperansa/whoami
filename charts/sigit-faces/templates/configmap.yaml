apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sigit-faces.fullname" . }}
  labels:
    {{- include "sigit-faces.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
