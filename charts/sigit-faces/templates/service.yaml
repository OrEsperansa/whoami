apiVersion: v1
kind: Service
metadata:
  name: {{ include "sigit-faces.fullname" . }}
  labels:
    {{- include "sigit-faces.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sigit-faces.selectorLabels" . | nindent 4 }}
